name: Build and Deploy Kanban Backend

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Validate secrets
      env:
        POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
        POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
        POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
        PORT: ${{ secrets.PORT }}
      run: |
        if [ -z "$POSTGRES_USER" ] || [ -z "$POSTGRES_PASSWORD" ] || [ -z "$POSTGRES_DB" ] || [ -z "$PORT" ]; then
          echo "❌ Error: One or more required secrets are not set!"
          echo "Please configure these secrets in Settings → Environments → kanban-backend:"
          echo "  - POSTGRES_USER"
          echo "  - POSTGRES_PASSWORD"
          echo "  - POSTGRES_DB"
          echo "  - PORT"
          exit 1
        fi
        echo "✅ All required secrets are configured"
    

    
    - name: Create .env file from secrets
      env:
        POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
        POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
        POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
        PORT: ${{ secrets.PORT }}
        DATABASE_URL: ${{ secrets.DATABASE_URL }}
      run: |
        echo "POSTGRES_USER=$POSTGRES_USER" > .env
        echo "POSTGRES_PASSWORD=$POSTGRES_PASSWORD" >> .env
        echo "POSTGRES_DB=$POSTGRES_DB" >> .env
        echo "PORT=$PORT" >> .env
        echo "DATABASE_URL=$DATABASE_URL" >> .env

    - name: Start services with Docker Compose
      run: |
        docker compose -f docker-compose.yml up -d --build --wait
        echo "Services started successfully."

    - name: Check service health
      env:
        PORT: ${{ secrets.PORT }}
      run: |
        echo "Testing API endpoint at http://localhost:$PORT/api"
        curl -v -f http://localhost:$PORT/api || exit 1

    - name: Debug Logs on Failure
      if: failure()
      run: |
        docker compose logs postgres
        docker compose logs backend

    - name: Cleanup (PRs or Failure)
      if: always() && (github.event_name == 'pull_request' || job.status == 'failure')
      run: |
        echo "Cleaning up test environment or failed deployment..."
        docker compose down
        rm -f .env

    - name: Deployment Success (Main Branch)
      if: success() && github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: |
        echo "✅ Deployment successful! Services remain running."
        docker compose ps
